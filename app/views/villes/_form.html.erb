<%= form_with(model: ville) do |form| %>
  <% if ville.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ville.errors.count, "error") %> prohibited this ville from being saved:</h2>

      <ul>
        <% ville.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
